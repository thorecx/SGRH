@model IEnumerable<SistemaGestorRecursosHumanos.Models.entradaEmpleados>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.empleados.codigoEmpleado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empleados.codigoEmpleado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_entrada }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_entrada }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_entrada })
        </td>
    </tr>
}

</table>
