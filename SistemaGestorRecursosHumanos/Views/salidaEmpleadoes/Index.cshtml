@model IEnumerable<SistemaGestorRecursosHumanos.Models.salidaEmpleado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipoSalida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.motivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaSalida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.empleados.codigoEmpleado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipoSalida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.motivo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaSalida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.empleados.codigoEmpleado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_salida }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_salida }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_salida })
        </td>
    </tr>
}

</table>
